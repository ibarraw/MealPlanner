CREATE TABLE meal(
	id LONG PRIMARY KEY AUTO_INCREMENT,
	drink VARCHAR(255),
	mainDish VARCHAR(255),
	treat VARCHAR(255),
	dayOfWeek VARCHAR(255),
	description VARCHAR(255),
	inStock VARCHAR(255)
);